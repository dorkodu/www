// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
  /* config options here */
  output: "export",
  distDir: "dist",
  reactStrictMode: true,
};

module.exports = nextConfig;
