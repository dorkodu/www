import {
  Image,
  Text,
  Container,
  ThemeIcon,
  Title,
  SimpleGrid,
  createStyles,
  rem,
  Box,
  Card,
  Paper,
} from "@mantine/core";

const styles = {
};

export function UseCaseCard({ icon, title, description, image }: UseCase) {}
