import { FunctionComponent } from "react";
import { WorkRole } from "../../types/dorkodu";

const Role: FunctionComponent<{
  role: WorkRole;
}> = ({ role }) => {
  return (
    <>
      <div></div>
    </>
  );
};

export default Role;
