import { Container } from "@mantine/core";
import { FunctionComponent } from "react";

const About: FunctionComponent = () => {
  return <Container size={1000} py={20}></Container>;
};

export default About;
